[project]
name = "flickr-download"
version = "0.1.0"
description = "Download all photos from a Flickr account at original resolution"
requires-python = ">=3.12"
dependencies = [
    "flickrapi>=2.4.0",
    "icecream>=2.1.1",
    "python-dotenv>=0.19.0",
    "requests>=2.33.0",
]
